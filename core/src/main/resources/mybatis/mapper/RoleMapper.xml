<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mirea.homedepot.catalogservice.core.repository.RoleRepository">

    <resultMap id="RoleEntity"
               type="com.mirea.homedepot.catalogservice.core.model.entity.RoleEntity" autoMapping="true">
        <id property="id" column="id" jdbcType="BIGINT"/>
    </resultMap>

    <select id="findAll" resultMap="RoleEntity">
        SELECT *
        FROM ctl.role;
    </select>

    <select id="findById" resultMap="RoleEntity">
        SELECT *
        FROM ctl.role
        WHERE id = #{id, jdbcType=BIGINT};
    </select>

    <select id="findByListId" resultMap="RoleEntity">
        <bind name="" value=""/>
        SELECT *
        FROM ctl.role
        <choose>
            <when test="listId.isEmpty()">
                ORDER BY id DESC
                LIMIT 10;
            </when>
            <otherwise>
                WHERE id IN
                <foreach collection='listId' item='id' open='(' separator=',' close=')'>
                    #{id, jdbcType=BIGINT}
                </foreach>
            </otherwise>
        </choose>
    </select>

    <select id="findByName" resultType="com.mirea.homedepot.commonmodule.model.Entity">
        SELECT *
        FROM ctl.role
        WHERE name = #{name, jdbcType=VARCHAR};
    </select>

    <select id="getListRoleByUserId" resultMap="RoleEntity">
        SELECT r.id, r.name
        FROM ctl.user_role
                 INNER JOIN role r on r.id = user_role.role_id
        WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO ctl.role (name)
        VALUES (#{name});
    </insert>

    <insert id="insertList" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO ctl.role (name) VALUES
        <foreach collection="entityList" item="entity" separator=",">
            (#{name})
        </foreach>
    </insert>

    <update id="update" parameterType="com.mirea.homedepot.catalogservice.core.model.entity.RoleEntity">
        UPDATE ctl.role SET
        <if test="name != null">
            name = #{name},
        </if>
        WHERE id = #{id};
    </update>

    <delete id="deleteById">
        DELETE
        FROM ctl.role
        WHERE id = #{id};
    </delete>
</mapper>

